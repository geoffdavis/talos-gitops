# Removed duplicate Longhorn ingress - using internal ingress only
# The internal ingress in infrastructure/longhorn/ingress.yaml handles Longhorn UI access
# with proper authentication and internal DNS configuration
